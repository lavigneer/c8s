kind: Service
metadata:
  name: c8s-webhook
  namespace: c8s-system
  labels:
    app: c8s-webhook
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: c8s-webhook
  sessionAffinity: None
---
